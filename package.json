{
  "name": "ryopc-home",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx @11ty/eleventy --clean",
    "test": "npm run build && npx eslint ."
  },
  "engines": {
    "node": ">=18.18.0"
  },
"devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "eslint": "^9.0.0"
  }
}