{
  "name": "ryopc-home",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx @11ty/eleventy --clean",
    "test": "npm run build && npx eslint ."
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "eslint": "^9.0.0"
  }
}
